<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head') %>
  <body class="flex min-h-screen items-center justify-center bg-surface p-6 text-on-surface">
    <main class="w-full max-w-lg rounded-xl border border-outline-variant bg-surface-container-lowest p-8 text-center shadow-card">
      <%- typeof body !== 'undefined' && body ? body : '<h1 class="font-display text-2xl font-bold text-primary">Something went wrong</h1><p class="mt-3 text-on-surface-variant">The page could not be loaded.</p>' %>
    </main>
  </body>
</html>
