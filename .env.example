# Hostinger production should use NODE_ENV=production
NODE_ENV=
# PORT is usually provided by hosting, but can be set to 3000 if required
PORT=

# DATABASE_URL is the Supabase PostgreSQL connection string
DATABASE_URL=

# SUPABASE_URL is the Supabase project URL
SUPABASE_URL=
# SUPABASE_ANON_KEY is the public anon key
SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY must only be used on the server
SUPABASE_SERVICE_ROLE_KEY=

# SESSION_SECRET must be a long random secret
SESSION_SECRET=

# APP_BASE_URL is the live website URL
APP_BASE_URL=

# Base domain for subdomain tenant resolution (local dev, e.g. localhost)
APP_BASE_DOMAIN=localhost

# Comma-separated hostnames that map to the platform default tenant
PLATFORM_HOSTS=localhost,127.0.0.1

# Storage buckets (paths always start with tenant slug, e.g. cda-kafue/programs/photo.jpg):
# - tenant-media (public): approved images, galleries, program photos, logos
# - resources (public): downloadable PDFs (reports, policies, publications)
# - private-documents (private): consent forms, child-related pending media, internal docs

# Optional local admin bypass (never enable in production)
DEV_AUTH_ENABLED=false
DEV_AUTH_USER_ID=dev-user
DEV_AUTH_EMAIL=dev@example.com
DEV_AUTH_ROLE=tenant_admin
DEV_AUTH_TENANT_ID=cda-kafue
